/* • Implemente un método llamado calcularResultado que
reciba por parámetros los dos números y la opción y
retorne el resultado de la operación*/

let readlineSync = require ("readline-sync");
let numero1 = readlineSync.questionInt("ingrese numero 1: ");
let numero2 = readlineSync. questionInt ("ingrese numero 2: ");
let opcionMenu = readlineSync.questionInt("ingrese 1 para sumar, 2 para restao u otro numero para salir: ");

fuction calcularResultado(numero1,numero2,opcionMenu){
if (opcionMenu == 1){

}
}